import 'package:flutter/material.dart';

class RecycleTipDialog extends StatelessWidget {
  const RecycleTipDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
